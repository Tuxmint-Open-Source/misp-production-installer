# Documentation-only CI smoke test

This temporary branch verifies that the proposed Repository gates workflow runs for a pull request whose diff contains documentation only. It is not intended to be merged.
